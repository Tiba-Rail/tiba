node_modules/
.next/
# Every env file (vercel env pull writes .env.local / .env.*.local / custom names). Only the
# placeholder template is tracked; CI fails if any other .env* file is committed.
.env*
!.env.example
*.log
*.webm
recordings/
dev.db
tsconfig.tsbuildinfo
scripts/free-disk.ps1

.vercel
.gstack/
scripts/.t3-agent-state.json
